import {createTheme} from "@mui/material/styles";

const aqTheme = createTheme({
  palette: {
    primary: {
      // main: "#20BFC3",
      main: "#20BFC3",
      contrastText: "white"
    },
  },
});

export default aqTheme