import {createTheme} from "@mui/material/styles";

const aqTheme = createTheme({
  palette: {
    primary: {
      main: "#20BFC3",
      light: "#E6FBFB",
      dark: "#005D5E",
      contrastText: "white"
    },
  },
});

export default aqTheme